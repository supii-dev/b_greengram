<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.greengram.application.feed.FeedMapper">
    <select id="findAllLimitedTo">
        SELECT f.feed_id,f.contents,f.location,f.created_at,f.writer_user_id,u.uid AS writerUid,u.nick_name AS writerNickName,u.pic AS writerPic,fl.feed_id IS NOT NULL AS isLike
        FROM feed AS f
        JOIN user AS u
        ON f.writer_user_id = u.user_id

        LEFT JOIN feed_like fl
        ON fl.feed_id = f.feed_id
        AND fl.user_id = #{signedUserId}

        ORDER BY f.feed_id DESC
        LIMIT #{startIdx}, #{size}
    </select>
    <select id="findAllPicByFeedId">
        SELECT pic FROM feed_pic
        WHERE feed_id = #{feedId}
    </select>

</mapper>